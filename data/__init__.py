"""Canonical schema/gold-mapping data: `source_schema.py`, `dest_schema.py`,
`gold_mapping.py` — the hand-transcribed ground truth everything else
(`pipeline/`, `scripts/generate_db_init.py`) reads from, never reverse-derives.
"""
