"""One-off generator scripts. Currently just `generate_db_init.py`, which
renders the Docker seed scripts (`docker/mysql-init/`, `docker/mongo-init/`)
from `data/source_schema.py` / `data/dest_schema.py`.
"""
